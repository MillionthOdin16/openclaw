## 2026-06-09 - Add ARIA labels to textual icon buttons

**Learning:** Icon-like buttons using text characters (like "×") need explicit ARIA labels just as much as SVG icon buttons do for screen reader accessibility.
**Action:** Always add aria-label to buttons that only contain textual symbols like "×" or "+".
