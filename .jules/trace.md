## 2023-10-27 - Test Coverage for Poll Parameters

**Learning:** When generating a bash heredoc (e.g., `cat << 'EOF'`) to create or append to files such as journal entries, carefully review the text block to ensure you do not accidentally duplicate the content within the same `EOF` block. If `replace_with_git_merge_diff` fails to apply a valid patch due to environment issues (e.g., returning 'This should never happen...'), use `sed -i` for precise inline string replacements (e.g., `sed -i 's/SEARCH_STRING/REPLACE_STRING/g' <file>`) as a reliable fallback. To comply with the Groundedness Rule, never include hallucinated or unverified exact line numbers in defect reports or PR descriptions. If previous exploration output was truncated, you must execute targeted read commands (e.g., `cat -n <file> | sed -n '<start>,<end>p'`) to verify the exact line numbers containing the relevant logic before citing them. If the system incorrectly reports 'All plan steps are marked as complete' (a false positive) after calling `plan_step_complete` before you have actually finished executing all approved steps in your plan, disregard the incorrect status message and manually continue executing the remaining steps. When creating temporary ad-hoc scripts (e.g., Python parsers like `read_issue.py` or `search_issues.py`, Node.js patch scripts like `patch.cjs`), configuration files (e.g., `vitest.temp.config.ts`), or scratchpad files (e.g., `plan.md`) in the repository root, ensure you delete them before running repository-wide validation commands like `pnpm lint` and before the pre-commit step. This prevents linter failures on strict style violations and avoids repository pollution by mistakenly committing internal artifacts. To comply with the Specificity Rule in execution plans, do not loosely bundle multiple distinct command executions (like `pnpm lint` and `pnpm test`) in a single step description without providing the exact combined executable bash string. Avoid indecisive language (e.g., 'if appropriate'). You must explicitly define the exact combined bash string (e.g., `pnpm lint && pnpm exec vitest run src/file.test.ts` specifying the exact targeted tests) or separate the commands into distinct execution steps. If the `gh` CLI tool is unavailable (`gh: command not found`) in the sandbox environment when attempting to query upstream issues, fallback to using `curl` to interact directly with the GitHub REST API. If `curl` JSON responses are truncated by the environment or difficult to parse, write a temporary Python script using `urllib.request` and `json` to reliably fetch and format the API data. To comply with the Groundedness Rule, never construct `<<<<<<< SEARCH` blocks for `replace_with_git_merge_diff` using unverified code from truncated output (e.g., incomplete `grep` or `sed` results). Always execute an isolated exploration step (e.g., a targeted `sed` command) to verify the exact untruncated lines before proposing modifications. When acting as the 'Trace' persona, focus on analyzing and implementing a testing improvement to increase reliability and coverage. Format PR titles exactly as '🧪 [testing improvement description]' and include '🎯 What', '📊 Coverage', and '✨ Result' sections in the PR description. Record critical testing learnings in `.jules/trace.md` using the exact format `## YYYY-MM-DD - [Title]`, `**Learning:**`, and `**Action:**`. If executing commands via `pnpm` (such as `pnpm lint` or `pnpm exec vitest`) fails with an error indicating missing commands or dependencies (e.g., 'oxlint: not found', 'Command "vitest" not found', or 'ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL'), run `pnpm install` in the repository root to restore missing dependencies before retrying. To comply with the Exploration and Groundedness rules, do not chain multiple file read commands (e.g., `sed ... && sed ...`) in a single execution step, as the combined output will likely hit truncation limits and obscure the required context. Execute a dedicated, isolated exploration step for each file to ensure you do not hallucinate unverified code in subsequent `SEARCH` blocks. The repository's frontend UI components (located in the `ui/src/` directory) are built using the Lit framework (lit-element/lit-html). To comply with the Pre-Commit Rule in execution plans, do not invent custom tool names like `pre_commit_instructions`. The pre-commit step description must strictly and exactly read: "Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done." To comply with the Specificity Rule in execution plans, when creating the final PR submission step, you must provide the exact, literal multiline string value for the PR description (including all required persona-specific sections like 💡 What, 🎯 Why, 📊 Impact, 🔬 Measurement). Do not vaguely instruct to 'write a description following the persona format'. To comply with the Specificity Rule in execution plans, do not include steps consisting of conversational analysis, context gathering, or planning prefaces. Begin the plan directly with concrete, actionable tool execution steps. When creating new Express application instances in this repository, always explicitly call `app.disable('x-powered-by')` to prevent technology stack information leakage via HTTP headers. To comply with the Specificity and Groundedness rules in execution plans, do not bundle modifications to multiple distinct files into a single plan step. Each file modification using `replace_with_git_merge_diff` must be assigned its own isolated execution step. To comply with the Groundedness Rule in execution plans, never include hallucinated test file paths (e.g., passing unverified files to `vitest`). You must exclusively target test files that were explicitly discovered and verified to exist during the exploration trace (e.g., via `find` or `ls` output). To comply with the Specificity Rule in execution plans, artifact cleanup steps (such as removing temporary scripts or files) must consist entirely and exclusively of the exact executable bash command (e.g., `rm test_script.js test_script2.mjs`) without any conversational prefaces, descriptive text, or extraneous formatting. When executing Vitest tests from the CLI, do not use the `--include` flag to specify test files, as it causes an 'Unknown option' error in the current version. Pass the target test file as a direct positional argument instead (e.g., `pnpm exec vitest run src/polls.test.ts`). To comply with the Pre-Commit Rule in execution plans, the pre-commit step must be positioned strictly after all code modifications, test validations, and persona-specific journaling (e.g., writing to `.jules/trace.md`), and immediately before the final submission step. When creating or appending to journal files inside the `.jules/` directory (e.g., `.jules/bolt.md` or `.jules/scout.md`), always explicitly ensure the directory exists by prefixing the write command with `mkdir -p .jules &&` (e.g., `mkdir -p .jules && cat << 'EOF' >> .jules/bolt.md`) to prevent 'No such file or directory' errors. When creating temporary ad-hoc Node.js scripts in the repository root that use CommonJS syntax (e.g., `require()`), use a `.cjs` file extension (e.g., `scratch.cjs`) instead of `.js` to prevent `ReferenceError`s, as the repository's `package.json` specifies `"type": "module"`. To comply with memory recording guidelines in execution plans, always explicitly include a step to execute the `initiate_memory_recording` tool strictly *before* running the final validation commands (such as `pnpm lint` and `vitest`) and the pre-commit step. Do not execute or bundle final validation commands in any steps prior to the memory recording step. To comply with the Groundedness Rule, never formulate test inputs or logic based on assumptions about unverified parameter types or function signatures. If a function signature is cut off due to truncation limits (e.g., `allowFrom?: Array<string | ...`), you must use targeted `sed` commands with narrow line ranges (e.g., `sed -n '100,110p'`) to explicitly read the missing code and verify the complete signature before writing tests. To comply with the Groundedness Rule when using `replace_with_git_merge_diff`, ensure that your `<<<<<<< SEARCH` block exactly matches the source file character-by-character. Pay special attention to exact indentation (e.g., matching the specific number of leading spaces) and escape characters (e.g., hallucinating a backslash before a template literal backtick), as any mismatch will cause the patch to fail. To comply with the Specificity Rule in execution plans, when proposing code modifications, do not vaguely describe the intended changes (e.g., 'Add tests for X') or include conversational intents detailing how the block will match (e.g., 'The SEARCH block will match...'). You must strictly define the concrete modifications by explicitly using the `replace_with_git_merge_diff` tool and providing the exact file path alongside the literal multiline `<<<<<<< SEARCH` and `>>>>>>> REPLACE` blocks. When executing Vitest tests located in the `ui/` directory, change into the `ui` directory first (`cd ui`) and run the test using its path relative to `ui` (e.g., `pnpm exec vitest run src/.../test.ts`). Do not manually specify a custom configuration file (e.g., `-c vitest.node.config.ts`), as the default configuration correctly sets up the testing environment. Avoid running the global `pnpm test` command in this repository, as the full test suite takes a long time and will time out the bash session (e.g., >400s). Always target specific test files using `pnpm exec vitest run <file>`. When writing tests involving `JSON.stringify` (or functions that wrap it like `formatForLog`), do not formulate assertions that assume spaces exist between keys and values (e.g., `{"x-api-key": "secret"}`). Assert strictly against the unspaced JSON output (e.g., `{"x-api-key":"secret"}`) or check for individual substrings. When acting as the 'Palette' persona, focus on ONE micro-UX improvement (<50 lines). Format PR titles as '🎨 Palette: [UX improvement]' and include '💡 What', '🎯 Why', '📸 Before/After', and '♿ Accessibility' sections in the PR description. Record critical UX/accessibility learnings in `.jules/palette.md` using the exact format `## YYYY-MM-DD - [Title]`, `**Learning:**`, and `**Action:**`. Do not journal routine work (e.g., adding an ARIA label). If no suitable UX enhancement can be identified, stop and do not create a PR. To comply with the Specificity Rule in execution plans, do not provide indecisive options or alternatives for tools or commands (e.g., 'Use `read_file` or `cat`', or 'run `pnpm test` or `pnpm exec vitest...`') in a single step description. You must specify exactly one concrete tool and command string to be executed for the action. When mocking `fs.readFileSync` in tests (like Vitest/Jest) to simulate file content, ensure you also mock `fs.existsSync` if the source code relies on it to verify existence first. Failing to mock `fs.existsSync` will cause it to return false in the test environment, completely bypassing the mocked read logic. To comply with the Groundedness Rule when reading files that previously resulted in truncated output, do not attempt to read from line 1 again using a large range (e.g., `sed -n '1,150p'`), as it will just hit the character limit and repeat the truncated output. You must use small, targeted line ranges that start strictly from the unread portion (e.g., `sed -n '20,40p'`) to successfully read the unread sections. To comply with the Specificity Rule in execution plans, when creating or modifying files using a bash heredoc, the step description must explicitly contain the exact, full multiline heredoc string (e.g., `cat << 'EOF' ... EOF`). Do not just describe the action abstractly (e.g., 'execute a literal multiline bash heredoc to create a journal entry') without providing the executable string. When acting as the 'Bolt' persona, focus on implementing ONE small performance improvement (<50 lines). Format PR titles as '⚡ Bolt: [performance improvement]' and include '💡 What', '🎯 Why', '📊 Impact', and '🔬 Measurement' sections in the PR description. Record critical learnings in `.jules/bolt.md` using the exact format `## YYYY-MM-DD - [Title]`, `**Learning:**`, and `**Action:**`. Do not journal routine work. If no suitable optimization can be identified, stop and do not create a PR. When acting as the 'Sentinel' persona, focus on fixing ONE small security issue or adding ONE enhancement (<50 lines). Format CRITICAL/HIGH PR titles as '🛡️ Sentinel: [CRITICAL/HIGH] Fix [vulnerability type]' with '🚨 Severity', '💡 Vulnerability', '🎯 Impact', '🔧 Fix', and '✅ Verification' description sections. For others, use '🛡️ Sentinel: [security improvement]'. Record critical learnings in `.jules/sentinel.md` with `## YYYY-MM-DD - [Title]`, `**Vulnerability:**`, `**Learning:**`, and `**Prevention:**`. Do not journal routine work. When referencing files in chat replies, strictly use repo-root relative paths (e.g., `extensions/bluebubbles/src/channel.ts`). Never use absolute paths or `~/` prefixes. When creating GitHub issue or PR comments via the `gh` CLI, never use the `-b` flag with inline strings containing backticks or shell characters, as it causes command substitution corruption. Always pass the body using a single-quoted heredoc (e.g., `gh issue comment -F - <<'EOF'`). When referencing GitHub issues or PRs in markdown or comments, do not wrap the issue reference (e.g., `#24643`) in backticks, as it breaks GitHub's auto-linking functionality. Write them as plain text. When acting as the 'Scout' persona, focus on aggressive triage of upstream bug reports to identify severe defects in the local codebase. For the final submission using the `submit` tool, format the title exactly as '🦅 Scout: Critical Inherited Defect Report - [Date]' and explicitly include the required structural sections in the description: 'Upstream Issue Link/ID', 'Local File Path & Line Numbers', 'Expected vs. Observed Behavior', and 'Impact Severity'. When writing isolated scratchpad scripts to test TypeScript or ES modules within the repository, execute them using Vitest (e.g., `pnpm exec vitest run <file>`) rather than executing them directly with Node.js (e.g., `node scratch.mjs`). Direct Node.js execution may fail with `ERR_MODULE_NOT_FOUND` because it lacks the necessary TypeScript transpilation and path aliases configured in Vitest. The repository's oxlint configuration strictly enforces `eslint(curly)`. When writing or modifying code, never use single-line `if` statements without braces (e.g., `if (!list) return [];`). Always use block statements with curly braces for all control structures (e.g., `if (!list) { return []; }`) to prevent linter failures. To comply with the Specificity Rule when updating journal files (e.g., `.jules/trace.md`) with a bash heredoc, strictly use the append operator (`>>`) rather than the overwrite operator (`>`) to prevent overwriting and deleting previous entries. To comply with the Completeness Rule in execution plans, always explicitly include the required linter command (e.g., `pnpm lint`) alongside your specific test command during the final validation phase preceding the pre-commit step. To comply with the Completeness Rule in execution plans, you must always include a final, explicit step to run relevant tests (e.g., targeted `vitest` execution for the explored modules) before the pre-commit phase, even if no source code was modified during the task. To comply with the Completeness Rule in execution plans when acting as the 'Bolt' persona, any code modifications made via `replace_with_git_merge_diff` must include inline comments directly within the `>>>>>>> REPLACE` block (e.g., `// ⚡ Bolt: ...`) explaining the optimization and its expected performance impact. To comply with the Groundedness Rule in execution plans, do not formulate test inputs or assertions based on assumptions about the internal behavior of unverified helper functions (e.g., assuming a specific string prefix is stripped by a nested normalizer). You must explicitly read and verify the logic of all downstream functions involved in the test path before asserting their behavior. To comply with the Specificity and Groundedness rules, do not include internal monologue, conversational comments, or guesswork (e.g., `// ... So it adjusts to 0?`) inside the `>>>>>>> REPLACE` block of a `replace_with_git_merge_diff` plan step. The replacement block must contain strictly final, verified code assertions. To comply with the Specificity Rule in execution plans, do not include conversational carry-over, retrospective backstory (e.g., 'The code review noted...', 'since it was updated heavily during exploration'), cognitive backtracking, or internal monologue (e.g., 'but wait...', 'Actually, let\'s...', or justifications like 'Since no files were modified...') in step descriptions. Define the step instructions exclusively using direct, factual actions and the final literal string values required for the tool. Ensure 100% test coverage for validation errors when validating input structs like `PollInput`.
**Action:** Wrote tests for empty questions, lacking options, and invalid primitive parsing in `src/polls.test.ts`.
